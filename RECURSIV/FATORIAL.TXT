PROCEDIMENTO FATORIAL (INTEIRO:NUMERO) {CALCULA O FATORIAL DE UM NUMERO}
    SE (NUMERO <= 1)
        IMPRIMA 1;
    SENÃO
        IMPRIMA NUMERO * FATORIAL(NUMERO-1);
    FIM-SE
FIM PROCEDIMENTO

INICIO
    INTEIRO: NUMERO;
    RECEBA(NUMERO);
    FATORIAL(NUMERO);
FIM
